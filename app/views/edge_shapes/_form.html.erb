<%= simple_form_for @edge_shape, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                edge_shapes_path, :class => 'btn' %>
  </div>
<% end %>
