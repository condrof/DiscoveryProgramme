<%= simple_form_for @macro, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :macro_id %>
  <%= f.input :total %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                macros_path, :class => 'btn' %>
  </div>
<% end %>
