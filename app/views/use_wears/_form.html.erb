<%= simple_form_for @use_wear, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                use_wears_path, :class => 'btn' %>
  </div>
<% end %>
