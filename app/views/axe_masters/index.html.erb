<%- model_class = AxeMaster -%>

<%= render 'search_form' %>
<div class="page-header">
	<h1><%= t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
	<thead>
		<tr>
			<th><%= sort_link @q, :name, "Seq No" %></th>
			<th><%= sort_link @q, :name, "museam_ref" %></th>
			<th><%= sort_link @q, :name, "collections_id" %></th>
			<th><%= sort_link @q, :name, "current_location" %></th>
			<th><%= sort_link @q, :name, "townland" %></th>
			<th><%= sort_link @q, :name, "area" %></th>
			<th><%= sort_link @q, :name, "county" %></th>
			<th><%= sort_link @q, :name, "discovery_circumstances" %></th>
			<% if can? :update, AxeMaster %>
			<th><%= t '.actions', :default => t("helpers.actions") %></th>
			<% end %>
		</tr>
	</thead>
	<tbody>
		<% @axe_masters.each do |axe_master| %>
		<tr>
			<% if axe_master.confirmed %>
				<td><%= link_to axe_master.seq_no, axe_master_path(axe_master) %></td>
				<td><%= axe_master.museam_ref %></td>
				<td><%= axe_master.collections_id %></td>
				<td><%= axe_master.current_location %></td>
				<td><%= axe_master.townland %></td>
				<td><%= axe_master.area %></td>
				<td><%= axe_master.county %></td>
				<td><%= axe_master.discovery_circumstances %></td>
				<% if can? :update, axe_master %>
					<td> <%= link_to t('.edit', :default => t("helpers.links.edit")),
					edit_axe_master_path(axe_master), :class => 'btn btn-mini' %>
				<% end %>
				<% if can? :destroy, axe_master %>
					<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
					axe_master_path(axe_master),
					:method => :delete,
					:confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
					:class => 'btn btn-mini btn-danger' %> </td>
				<% end %>
			<% end %>
		</tr>
		<% end %>
	</tbody>
</table>

<% if can? :create, AxeMaster %>
	<%= link_to t('.new', :default => t("helpers.links.new")),
	new_axe_master_path,
	:class => 'btn btn-primary' %>
<% end %>
