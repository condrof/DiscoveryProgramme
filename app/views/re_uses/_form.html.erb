<%= simple_form_for @re_use, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :description %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                re_uses_path, :class => 'btn' %>
  </div>
<% end %>
